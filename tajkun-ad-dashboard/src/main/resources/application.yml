server:
  port: 7002

spring:
  application:
    name: tajkun-ad-dashboard

eureka:
  client:
    service-url:
      defaultZone: http://server1:8000/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"  # 暴露全部监控信息


